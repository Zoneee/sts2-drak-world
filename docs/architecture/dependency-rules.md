# 依赖规则

状态：有效
负责人：工程团队
最后评审：（填写日期）

## 允许的依赖
- 类型层 → 无，或仅依赖原语
- 配置层 → 类型层
- 数据层 → 类型层、配置层
- 服务层 → 类型层、配置层、数据层
- 运行时层 → 服务层、数据层、配置层、类型层
- UI 层 → 服务层、运行时层适配器、类型层

## 禁止的依赖
- UI 层 → 数据层
- 服务层 → UI 层
- 数据层 → UI 层
- 领域间循环依赖
- 绕过校验边界

## 验证方式
这些规则应尽可能由机械手段检查。
