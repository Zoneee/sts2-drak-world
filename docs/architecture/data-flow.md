# 数据流

状态：草稿
负责人：工程团队
最后评审：（填写日期）

## 用途
描述系统中关键数据的流转路径，包括输入来源、处理节点、输出去向。

## 关键流程

### 请求处理流
（待填写：HTTP 请求 → 路由 → 服务层 → 数据层 → 响应）

### 后台任务流
（待填写）

### 事件/Webhook 流
（待填写）

## 数据边界标注
- 边界进入点：所有外部输入必须经过校验
- 边界退出点：敏感数据必须脱敏后输出
