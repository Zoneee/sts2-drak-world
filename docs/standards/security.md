# 安全规范

状态：有效
负责人：工程团队
最后评审：（填写日期）

## 核心原则
- 验证所有不可信输入
- 最小权限原则
- 默认拒绝，显式授权
- 防御性编程

## 密钥管理
- 密钥不得硬编码在代码中
- 使用环境变量或密钥管理服务
- 定期轮换敏感凭证

## 认证与授权
- 所有 API 端点须明确定义认证要求
- 权限检查在服务层进行，不得仅依赖 UI 层
- 会话管理须有超时机制

## 日志安全
- 日志不得包含密码、Token、PII 数据
- 错误消息不得向外部暴露内部实现细节

## 依赖安全
- 定期扫描依赖漏洞
- 尽快修复高危漏洞
