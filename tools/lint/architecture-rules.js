/**
 * 架构规则 Lint 配置占位文件
 * 根据实际项目和使用的 Lint 工具（ESLint、自定义脚本等）替换此文件
 *
 * 示例规则（伪代码）：
 * - UI 层不得直接导入数据层模块
 * - 服务层不得导入 UI 层模块
 * - 数据层不得导入 UI 层模块
 */

module.exports = {
  rules: {
    // 示例：禁止 UI 层导入数据层
    // 'no-ui-to-repo': { ... }
  }
}
