/**
 * 日志规则 Lint 配置占位文件
 * 根据实际项目替换此文件
 *
 * 示例规则：
 * - 禁止 console.log 在生产代码中使用
 * - 禁止在日志中记录密码字段
 */

module.exports = {
  rules: {
    // 示例
    // 'no-console': ['warn', { allow: ['error', 'warn'] }]
  }
}
